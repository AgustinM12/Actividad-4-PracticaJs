let area, altura, base;
base = 5;
altura = 10;
function calcularAreaRectangulo(altura, base, area) {
    area = base * altura
    return (area)
}
console.log(calcularAreaRectangulo(altura, base, area))